@model FunlabProgramChallenge.ViewModels.LogInUserViewModel
<div class="navbar-collapse collapse d-sm-inline-flex justify-content-between">
    @if (!string.IsNullOrEmpty(Model.RoleName))
    {
        if (Model.RoleName == "Member")
        {
            <ul class="navbar-nav flex-grow-1">
                <li class="nav-item">
                    <a class="nav-link" href="@Url.Action("Index", "Home", new { Area = "" })">Home</a>
                </li>
            </ul>
        }
        else if (Model.RoleName == "Admin")
        {
            <ul class="navbar-nav flex-grow-1">
                <li class="nav-item">
                    <a class="nav-link" href="@Url.Action("Index", "Home", new { Area = "" })">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="javascript:void(0)" id="lnkGetMembers">Get Members</a>
                </li>
            </ul>
        }
        if (!string.IsNullOrEmpty(Model.UserName))
        {
            <div class="navbar-login float-right">
                Welcome, @Model.UserName <a class="btn btn-danger" href="@Url.Action("LogOut", "Admin", new { Area = "" })">Log Out</a>
            </div>
        }
    }
</div>